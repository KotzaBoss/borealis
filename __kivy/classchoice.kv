<ClassChoice>:

    GridLayout:
        cols: 3
        size: root.width, root.height

        Button:
            text: 'Barbarian'
            on_release:
                root.btn(self.text)
                app.root.current = 'init_roll'

        Button:
            id: Human
            text: 'Monk'
            on_release:
                root.btn(self.text)
                app.root.current = 'init_roll'

        Button:
            id: Gnome
            text: 'Rogue'
            on_release:
                root.btn(self.text)
                app.root.current = 'init_roll'

        Button:
            id: Dwarf
            text: 'Ranger'
            on_release:
                root.btn(self.text)
                app.root.current = 'init_roll'

        Button:
            id: Dragonborn
            text: 'Wizard'
            on_release:
                root.btn(self.text)
                app.root.current = 'init_roll'

        Button:
            id: Half_orc
            text: 'Sorcerer'
            on_release:
                root.btn(self.text)
                app.root.current = 'init_roll'

        Button:
            id: Tiefling
            text: 'Paladin'
            on_release:
                root.btn(self.text)
                app.root.current = 'init_roll'

